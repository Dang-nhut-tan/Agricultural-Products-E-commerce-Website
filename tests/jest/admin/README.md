# Jest Admin

Đặt unit test dành riêng cho cấu hình, helper và nghiệp vụ Admin trong thư mục này.

Các luồng CRUD AdminJS hiện được kiểm tra bằng Playwright tại `tests/playwright/admin/` vì cần tương tác giao diện và xác nhận dữ liệu thật trong database.
